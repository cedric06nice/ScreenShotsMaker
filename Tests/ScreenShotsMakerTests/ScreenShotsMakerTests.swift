import XCTest
@testable import ScreenShotsMaker

final class ScreenShotsMakerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ScreenShotsMaker().text, "Hello, World!")
    }
}
